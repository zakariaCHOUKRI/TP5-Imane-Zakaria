specVersion: "v1beta"
serviceId: "blog-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "blog-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
